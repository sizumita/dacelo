let helper arg =
  let rec loop y = y + 1000 in
  loop arg

let f n =
  let rec loop x =
    let tmp = helper 0 in
    if x == 0 then tmp else loop (x - 1) + tmp
  in
  loop n

let main () = print_int (f 2)
